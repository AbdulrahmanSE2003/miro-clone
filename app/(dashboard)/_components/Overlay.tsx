const Overlay = () => {
  return (
    <div
      className={`opacity-0 group-hover:opacity-30 transition-opacity h-full w-full bg-black duration-300`}
    >
      Overlay
    </div>
  );
};

export default Overlay;
